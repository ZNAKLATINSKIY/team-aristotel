const fun = (char) => {
    switch(char) {
        case "у" || "у" || "Е" || "е" || "Э" || "э" || "О" || "о" || "А" || "а" || "Ы" || "ы" || "Я" || "я" || "И" || "и" || "Ю" || "ю":
            alert("Гласная");
            break
        default:
            alert("Согласная.");
    }
}