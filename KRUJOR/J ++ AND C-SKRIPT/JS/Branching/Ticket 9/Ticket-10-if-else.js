const fun = (a, b, c) => {
    if ((a < b && b < c) || (a > b && b > c)) {
        a *= 2;
        b *= 2;
        c *= 2;
        alert(`Новые значения переменных: ${a}, ${b}, ${c}`);
    } else {
        a = -a;
        b = -b;
        c = -c;
        alert(`Новые значения переменных: ${a}, ${b}, ${c}`);
    }
}
